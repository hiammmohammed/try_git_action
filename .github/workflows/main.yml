on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest

    steps : 
      - uses: actions/checkout@v3   

      - name: 'Login to Docker hub'
        run: docker login --username haiam --password  dckr_pat_77tSD2fPhRahl6aU230BVkDPpVk

     