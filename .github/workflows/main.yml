on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest

    steps : 
      - name: 'Befor'
        run: ls

      - users: actions/checkout@v3   

      - name: 'After'
        run: ls

     